import json
import zipfile

class JsonFile:
    def __init__(self, data):
        self.data = data

# TODO: Create a DATATYPE, with parameters for instagram specific data, as the format of the data we will be recieving will always be the same.

def transform_zipfile(zip_file):
    pass